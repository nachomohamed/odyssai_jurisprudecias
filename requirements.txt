# Core datos / utilidades
pandas==2.2.2
numpy==1.26.4
python-dotenv==1.0.1
orjson==3.10.7
httpx==0.27.0

# LLM + embeddings + rerank
openai==1.46.0           # (>=1.40.0 para langchain-openai)
tiktoken==0.7.0
sentence-transformers==3.0.1
transformers==4.43.3
torch>=2.2,<3.0          # CPU; si tenés CUDA, podés instalar torch con la build CUDA

# Vector DB
chromadb==0.5.5
faiss-cpu==1.8.0.post1

# LangChain (núcleo + proveedores)
langchain==0.2.16
langchain-community==0.2.16
langchain-openai==0.1.23
# langsmith se trae como dependencia; no hace falta fijarlo

# Backend API
fastapi==0.111.0
uvicorn==0.30.1

# Pydantic (alineado a FastAPI y LangChain >=0.2)
pydantic==2.8.2
pydantic-settings==2.4.0

# Tenacity: rango compatible con chromadb y langchain
tenacity>=8.2.3,<9,!=8.4.0

# Frontend
streamlit
gdown
